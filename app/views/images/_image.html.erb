<% if @images.exists? %>
  <div class="responsive">
    <% @images.each do |image| %>
      <%= link_to image_tag(image.image_url), image_path(image), class: "img-responsive" %>
    <% end %>
  </div>
<% end %>
